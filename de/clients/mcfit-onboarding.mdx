---
title: 'Enterprise Onboarding Anleitung'
description: 'Vollständige Einrichtungsanleitung zur Integration unserer Analytics-Plattform in Ihre Unternehmensumgebung. Folgen Sie diesen Schritten zur Implementierung von Server-seitigem Tracking und Event-Tagging.'
---

<Note>
    **Geschätzte Einrichtungszeit:** max. 30 Minuten, abhängig von der Komplexität Ihrer Website
    **Erforderlicher Zugriff:** Entwicklerzugang zur Codebasis Ihrer Website
</Note>

---

## Phase 1: Script-Integration

Der erste Schritt besteht darin, unser Analytics-Script in Ihre Website zu integrieren.

<Note>
    Bitte beachten Sie, dass dies nur eine zusätzliche Ressource zum Onboarding im Produkt ist. Sie erhalten das Script für Ihre Website im Onboarding-Ablauf.
</Note>

### Installationsschritte

<Steps>
    <Step title="Finden Sie Ihr eindeutiges Script">
        Ihr eindeutiges Tracking-Script ist in Ihrem Dashboard unter **Einstellungen → Installation** verfügbar.

        Es sieht folgendermaßen aus:
        ```html
        <script defer src="https://analytics.bchic.de/script.js" data-website-id="YOUR_SITE_ID"></script>
        ```
        <Warning>
            **Wichtig:** Jede Domain benötigt ihre eigene eindeutige `data-website-id` Kennung. Teilen Sie dieses Script niemals über verschiedene Domains hinweg.
        </Warning>
    </Step>
    <Step title="Fügen Sie das Script zu Ihrer Website hinzu">
        Fügen Sie das Script im `<head>`-Bereich Ihres HTML ein, direkt vor dem schließenden `</head>`-Tag.
        <Tabs>
            <Tab title="Standard HTML">
                ```html index.html
                <!DOCTYPE html>
                <html lang="de">
                <head>
                    <meta charset="UTF-8">
                        <meta name="viewport" content="width=device-width, initial-scale=1.0">
                            <title>Ihre Website</title>
                            <!-- Weitere Head-Elemente -->

                            <!-- Analytics Script - Vor dem schließenden </head> hinzufügen -->
                            <script src="https://analytics.bchic.de/script.js"
                                    data-website-id="YOUR_SITE_ID"
                                    defer></script>
                </head>
                <body>
                <!-- Ihr Inhalt -->
                </body>
                </html>
                ```
            </Tab>

            <Tab title="React / Next.js">
                ```jsx _app.js or layout.js
                import Head from 'next/head';

                export default function App({ Component, pageProps }) {
                return (
                <>
                <Head>
                <script
                src="https://analytics.bchic.de/script.js"
                data-website-id="YOUR_SITE_ID"
                defer
                />
                </Head>
                <Component {...pageProps} />
        </>
        );
        }
        ```
    </Tab>
    <Tab title="WordPress">
        Fügen Sie diesen Code zur `header.php`-Datei Ihres Themes hinzu oder verwenden Sie ein Plugin wie "Insert Headers and Footers":
        ```html header.php
        <!-- Vor wp_head() oder im <head>-Bereich hinzufügen -->
        <script src="https://analytics.bchic.de/script.js"
                data-website-id="YOUR_SITE_ID"
                defer></script>
        <?php wp_head(); ?>
        ```
    </Tab>
    <Tab title="Google Tag Manager">
        1. Erstellen Sie ein neues **Custom HTML** Tag in GTM
        2. Fügen Sie den Script-Code ein
        3. Setzen Sie den Trigger auf **Alle Seiten**
        4. Veröffentlichen Sie Ihren Container
        ```html
        <script src="https://analytics.bchic.de/script.js"
                data-website-id="YOUR_SITE_ID"
                defer></script>
        ```
    </Tab>
</Tabs>
</Step>
<Step title="Installation überprüfen">
    Nachdem Sie das Script bereitgestellt haben, überprüfen Sie, ob es korrekt funktioniert, indem Sie auf die Schaltfläche im Onboarding-Ablauf klicken. bchic Analytics prüft automatisch, ob Daten verfügbar sind, und leitet Sie zum Dashboard weiter.
</Step>
</Steps>


## Events Anonymisieren (URL-Anonymisierung)

Ein sehr wichtiger Teil der datenschutzkonformen Einrichtung ist die **URL-Anonymisierung**. Da wir keine personenbezogenen Daten erfassen dürfen, müssen URLs, die persönliche Identifier enthalten (z.B. eine User-ID, ein Name oder eine Bestellnummer), anonymisiert werden. So können wir das allgemeine Nutzerverhalten auf Seitentypen analysieren, ohne Rückschlüsse auf eine konkrete Person ziehen zu können.

### Fallbeispiel: Anonymisierung einer dynamischen Profil-URL

Stellen Sie sich vor, Ihre Website hat eine URL-Struktur wie: `https://beispiel-shop.de/profil/12345/bestellungen`. Hier ist `12345` eine eindeutige `userId` und somit ein personenbezogenes Datum, das nicht erfasst werden darf.

### Lösung & Implementierung

Fügen Sie das `data-anonymize-url`-Attribut zu Ihrem bestehenden Script-Tag hinzu.

<CodeGroup>
    ```html title="Vorher: Standard-Script"
    <script
        defer
        src="https://analytics.bchic.de/script.js"
        data-website-id="IHRE_WEBSITE_ID"
    ></script>
    ```
    ```html title="Nachher: Script mit URL-Anonymisierung"
    <script
        defer
        src="https://analytics.bchic.de/script.js"
        data-website-id="IHRE_WEBSITE_ID"
        data-anonymize-url="/profil/{userId}/*"
    ></script>
    ```
</CodeGroup>

<Info title="Was wird erfasst?">
    Anstatt der vollständigen URL `https://beispiel-shop.de/profil/12345/bestellungen` wird in den Analytics nur die sichere und anonymisierte Version `/profil/{userId}/bestellungen` gespeichert.
</Info>

---

### Funktionsweise und Syntax

Das `data-anonymize-url`-Attribut ermöglicht es Ihnen, mit einer einfachen Mustersprache dynamische Teile Ihrer URLs zu maskieren. Unser Script erkennt diese Muster und ersetzt die entsprechenden Segmente, *bevor* die Daten an unsere Server gesendet werden.

Folgende Syntaxelemente können Sie verwenden:

- **`{platzhalter}`**: Geschweifte Klammern definieren einen dynamischen Teil der URL, der durch den Platzhalternamen (z.B. `{userId}`) ersetzt wird. Der Name dient nur der Lesbarkeit in Ihren Analytics-Berichten.
- **`*` (Wildcard)**: Der Stern agiert als "Catch-all" und sorgt dafür, dass alle nachfolgenden URL-Segmente unverändert erfasst werden. Er ist besonders nützlich, wenn die URL-Struktur nach dem dynamischen Teil variieren kann (z.B. `/profil/12345/einstellungen` und `/profil/12345/bestellungen`).

### Mehrere Regeln definieren

Wenn Sie mehrere unterschiedliche URL-Strukturen anonymisieren müssen, können Sie die Regeln einfach **durch ein Komma getrennt** im selben Attribut angeben.

**Beispiel:** Anonymisierung von Profil- und Bestell-Detailseiten.

```html
<script
    defer
    src="https://analytics.bchic.de/script.js"
    data-website-id="IHRE_WEBSITE_ID"
    data-anonymize-url="/profil/{userId}/*,/bestellungen/details/{orderId}"
></script>
```
In diesem Fall werden zwei URL-Typen behandelt:
1. Alle URLs, die mit `/profil/` beginnen und eine ID enthalten.
2. URLs für Bestelldetails (z.B. `/bestellungen/details/98765`).

---

### Weitere Anwendungsfälle

Hier sind einige weitere Beispiele, um die Flexibilität des Tools zu zeigen:

<CardGroup>
    <Card title="Fall 1: Anonymisierung eines öffentlichen Nutzernamens">
        - **Original-URL:** `https://beispiel-community.de/user/max-mustermann`
        - **Anonymisierte URL:** `/user/{username}`
        - **Implementierung:** `data-anonymize-url="/user/{username}"`
    </Card>
    <Card title="Fall 2: Anonymisierung einer Bestell-ID in der URL">
        - **Original-URL:** `https://beispiel-shop.de/checkout/success/order/B-1023-4567`
        - **Anonymisierte URL:** `/checkout/success/order/{orderId}`
        - **Implementierung:** `data-anonymize-url="/checkout/success/order/{orderId}"`
    </Card>
</CardGroup>

---

### Überprüfung und Best Practices

#### Wie kann ich die Funktion testen?

Am einfachsten können Sie die korrekte Funktion in den **Entwicklertools Ihres Browsers** überprüfen:
1. Öffnen Sie die Entwicklertools (meist mit `F12` oder `Cmd+Option+I`).
2. Wechseln Sie zum Tab **"Netzwerk"** (oder "Network").
3. Laden Sie die Seite neu, die eine anonymisierte URL enthält.
4. Suchen Sie in der Liste der Anfragen nach einem Aufruf, der an `analytics.bchic.de` geht (oft ein `POST`-Request).
5. Klicken Sie auf diesen Aufruf und prüfen Sie die **Payload** (die gesendeten Daten). Das dort übermittelte `url`-Feld sollte bereits die anonymisierte Form aufweisen.

<Info>
    **Best Practices:**
    - **Seien Sie proaktiv:** Gehen Sie die gängigsten URL-Strukturen Ihrer Webseite durch und prüfen Sie sie auf personenbezogene Daten. Typische Kandidaten sind Profilseiten, Bestellbestätigungen, Warenkörbe oder Seiten mit Session-IDs in der URL.
    - **Beginnen Sie spezifisch:** Definieren Sie Regeln so genau wie nötig. Ein zu allgemeiner Wildcard (`*`) könnte versehentlich auch für die Analyse wichtige Informationen maskieren.
</Info>

## Phase 2: Event-Tagging

Nachdem das Script läuft, besteht der nächste Schritt darin, bestimmte Elemente auf Ihrer Website zu taggen, damit die Analytics-Plattform Benutzerinteraktionen wie Button-Klicks, Formularübermittlungen und benutzerdefinierte Events tracken kann.

<Info>
    **Grundregeln für Events**
    - **Zeichenlimit:** Event-Namen sind auf 50 Zeichen begrenzt.
    - **Name erforderlich:** Events können nicht ohne einen Event-Namen gesendet werden.
</Info>

### Event-Typen verstehen

Unsere Plattform unterstützt zwei Arten von Event-Tracking:

<CardGroup cols={2}>
    <Card title="Property-Type Events" icon="tag">
        **Einfaches Event-Tracking**, das nur den Event-Namen sendet.
        Anwendungsfall: Tracken, dass ein Button geklickt wurde, ohne zusätzlichen Kontext.

        Beispiel: Newsletter-Anmelde-Button, Download-Link
    </Card>

    <Card title="Payload Events" icon="database">
        **Erweitertes Tracking**, das den Event-Namen plus zusätzliche nicht-personenbezogene Daten sendet.
        Anwendungsfall: Tracken, welches Produkt in den Warenkorb gelegt wurde, welcher Filter angewendet wurde.

        Beispiel: Produktkategorie, Suchbegriff, Filterauswahl
    </Card>
</CardGroup>

<Warning>
    **Kritische Datenschutzanforderung**
    Unsere Plattform ist so konzipiert, dass sie ohne personenbezogene Daten funktioniert. Verwenden Sie niemals:

    - E-Mail-Adressen
    - Namen oder Benutzernamen
    - Telefonnummern
    - IP-Adressen
    - Jegliche personenidentifizierbare Informationen (PII)

    **Warum das wichtig ist:** Die Einbeziehung personenbezogener Daten widerspricht dem Zweck unserer datenschutzfreundlichen Analytics und kann gegen Datenschutzbestimmungen verstoßen. Es beeinträchtigt auch die Fähigkeit der Plattform, genaue, aggregierte Einblicke zu liefern.

    **Was Sie senden KÖNNEN:** Produkt-IDs, Kategorienamen, Seitenabschnitte, Feature-Namen, nicht-personenbezogene Filterwerte, generische Labels.
</Warning>

### Methode 1: Event-Attribute in HTML hinzufügen

Events werden durch Hinzufügen von Datenattributen zu Ihren HTML-Elementen getrackt. Das Script erkennt diese Attribute automatisch und sendet die Daten an unsere Server.

<Steps>
    <Step title="Wählen Sie Ihren Event-Typ">
        Entscheiden Sie, ob Sie ein einfaches Property-Type Event oder ein Payload Event mit zusätzlichen Daten benötigen.

        **Entscheidungshilfe:**
        - Möchten Sie wissen, **OB** etwas passiert ist? → **Property-Type**
        - Möchten Sie wissen, **WAS** genau passiert ist? → **Payload Event**
    </Step>
    <Step title="Fügen Sie das Datenattribut hinzu">
        Fügen Sie das entsprechende Datenattribut zu Ihrem HTML-Element hinzu.
        <Tabs>
            <Tab title="Property-Type Event">
                **Syntax:** `data-bchic-event="event-name"`
                ```html
                <!-- Vorher: Nicht getrackter Button -->
                <button class="cta-button">
                    Whitepaper herunterladen
                </button>
                <!-- Nachher: Getrackt mit Property-Type Event -->
                <button class="cta-button"
                        data-bchic-event="whitepaper-download">
                    Whitepaper herunterladen
                </button>
                ```
                **Was wird getrackt:** Der Event-Name `whitepaper-download` wird beim Klicken gesendet.
            </Tab>

            <Tab title="Payload Event">
                **Syntax:** Verwenden Sie `data-bchic-event="{event-name}"` für den Namen und `data-bchic-event-{key}="{value}"` für die zusätzlichen Daten.
                ```html
                <!-- Vorher: Nicht getrackte Produktkarte -->
                <div class="product-card">
                    <h3>Premium Plan</h3>
                    <button class="add-to-cart">In den Warenkorb</button>
                </div>

                <!-- Nachher: Getrackt mit Payload Event -->
                <div class="product-card">
                    <h3>Premium Plan</h3>
                    <button class="add-to-cart"
                            data-bchic-event="add-to-cart"
                            data-bchic-event-product="premium-plan"
                            data-bchic-event-category="subscription">
                        In den Warenkorb
                    </button>
                </div>
                ```
                **Was wird getrackt:** Der Event-Name `add-to-cart` plus die Payload-Daten `{ "product": "premium-plan", "category": "subscription" }`.

                <Note>
                    **Wichtige Hinweise:**
                    - Alle über diese Methode gesendeten Daten werden als **String** gespeichert.
                    - Um numerische Werte, Booleans etc. zu speichern, verwenden Sie die JavaScript-Methode.
                    - **Andere Event-Listener** innerhalb desselben Elements werden bei dieser Methode **nicht ausgelöst**.
                </Note>
            </Tab>
        </Tabs>
    </Step>
    <Step title="Testen Sie Ihre Events">
        Nachdem Sie Event-Attribute hinzugefügt haben, testen Sie diese, um sicherzustellen, dass sie korrekt ausgelöst werden:
        Navigieren Sie zum Events-Bereich in bchic Analytics und sehen Sie sich die protokollierten Events an.

        Die Events sollten in Echtzeit erscheinen. Falls sie nicht sofort angezeigt werden, aktualisieren Sie einfach die Seite (**Command + R** / **Ctrl + R**), und Sie sollten Ihre Events sehen.
        <Note>
            Wir tracken Scroll-Tiefe und Outbound-Klicks bereits standardmäßig, sodass Sie hier nichts weiter einrichten müssen.
        </Note>
    </Step>
</Steps>

### Methode 2: Events mit JavaScript senden (Flexibler)

Sie können Events auch manuell mit dem `window.bchic.track()` Objekt auslösen. Diese Methode ist ideal für dynamische Daten und wenn Sie Datentypen wie Zahlen oder Booleans exakt übermitteln müssen.

<Tabs>
    <Tab title="Einfaches Event">
        ```javascript
        const button = document.getElementById('signup-button');

        button.onclick = () => {
        window.bchic.track('Signup button');
    };
        ```
    </Tab>
    <Tab title="Payload Event">
        ```javascript
        const button = document.querySelector('.add-to-cart');

        button.onclick = () => {
        window.bchic.track('add-to-cart', {
            product: 'premium-plan',
            price: 99.99, // Wird als Zahl gespeichert
            is_featured: true // Wird als Boolean gespeichert
        });
    };
        ```
    </Tab>
</Tabs>

## Best Practices für Event-Benennung

Befolgen Sie diese Konventionen, um Ihre Event-Daten organisiert und aussagekräftig zu halten:

<Info>
    Dies ist noch nicht verfügbar, aber in Zukunft wird es möglich sein, die Benennung einzelner Event-Namen zu ändern, was es Ihren Marketing-Teams ermöglicht, mit echten Namen anstelle von IDs zu arbeiten. Die Standardeinstellung bleibt immer die ID, kann aber mit einem benutzerfreundlicheren Namen versehen werden.
</Info>

<Tabs>
    <Tab title="Benennungskonventionen">
        **Verwenden Sie kebab-case für Event-Namen:**
        - ✅ `newsletter-signup`
        - ✅ `add-to-cart`
        - ✅ `video-play`
        - ❌ `Newsletter_Signup`
        - ❌ `addToCart`

        **Seien Sie spezifisch, aber prägnant:**
        - ✅ `pricing-cta-click`
        - ❌ `click`
        - ❌ `user-clicked-the-pricing-page-call-to-action-button`

        **Verwenden Sie konsistente Terminologie:**
        Wenn Sie an einer Stelle `signup` verwenden, nutzen Sie nicht woanders `register` für dieselbe Aktion.
    </Tab>

    <Tab title="Datenschutz-Checkliste">
        **Bevor Sie Daten senden, überprüfen Sie, dass diese KEINE personenbezogenen Daten (PII) enthalten:**

        - Enthält KEINE E-Mail-Adressen
        - Enthält KEINE Namen oder Benutzernamen
        - Enthält KEINE Telefonnummern
        - Enthält KEINE Standortdaten, die spezifischer als Land/Region sind
        - Enthält KEINE Benutzer-IDs oder Session-IDs in der **Event-Payload**
        - **Erfasste URLs** enthalten KEINE persönlichen Identifier (User-IDs, Namen etc.)
        - Enthält KEINE IP-Adressen (dies stellen wir serverseitig sicher)
        - Enthält NUR generische, aggregierbare Daten

        **Sichere Payload-Beispiele:**
        - Produktkategorien, SKUs oder generische Kennungen
        - Feature-Namen oder Abschnittsbezeichnungen
        - Nicht-personenbezogene Filterauswahlen
        - Inhaltstypen oder Formate
        - Generische Statusindikatoren
    </Tab>
</Tabs>

---
## Letzter Schritt: Team einladen

Nachdem Ihr Analytics-Tracking nun vollständig eingerichtet ist, besteht der letzte Schritt darin, Ihren Kollegen Zugriff zu gewähren. Dies ermöglicht es Stakeholdern, wie beispielsweise Ihrem Marketing-Team, die Daten zu nutzen und wertvolle Einblicke zu gewinnen.

### So fügen Sie Teammitglieder hinzu

Folgen Sie diesen einfachen Schritten, um Ihr Team auf der Plattform aufzubauen.

<Steps>
    <Step title="Zur Teamverwaltung navigieren">
        Gehen Sie in der Hauptnavigations-Seitenleiste links zum unteren Bereich **Einstellungen** und klicken Sie auf **Teamverwaltung**.

        <Frame>
            <img src="/images/team-management-sidebar.png" alt="Navigation zum Bereich Teamverwaltung in der Seitenleiste" />
        </Frame>
    </Step>

    <Step title="Ein neues Mitglied einladen">
        Auf der Seite „Teamverwaltung“ sehen Sie zwei Reiter: `Aktiv` und `Eingeladen`. Klicken Sie oben rechts auf den Button **Mitglied einladen**, um den Einladungsdialog zu öffnen.
    </Step>

    <Step title="Eine Rolle auswählen">
        Bevor Sie die Einladung versenden, müssen Sie dem neuen Mitglied eine Rolle zuweisen. Jede Rolle verfügt über spezifische Berechtigungen, die für unterschiedliche Zugriffsebenen konzipiert sind.

        <CardGroup>
            <Card title="Mitarbeiter/in" icon="user">
                **Begrenzter Zugriff**
                - ✅ Kann freigegebene Dashboards und Berichte ansehen.
                - ❌ Keine Einsicht in Abrechnungs- oder Nutzungsinformationen.
            </Card>
            <Card title="Manager" icon="users">
                **Erweiterter Zugriff**
                - ✅ Kann Teammitglieder verwalten (erstellen/bearbeiten).
                - ✅ Hat vollen Zugriff auf alle Dashboards und Analysen des Teams.
                - ❌ Kein Zugriff auf globale Organisationseinstellungen.
            </Card>
            <Card title="Admin" icon="shield-check">
                **Uneingeschränkter Zugriff**
                - ✅ Voller Zugriff auf alle Einstellungen und die Benutzerverwaltung.
                - ✅ Kann Datenquellen und Integrationen verwalten.
                - ✅ Vollständige Einsicht in alle Analysen und Nutzungsdaten.
            </Card>
        </CardGroup>

        <Tip>
            **Unsere Empfehlung:** Für die meisten Nutzer, wie z.B. Mitglieder Ihres Marketing-Teams, ist die Rolle **Mitarbeiter/in** die perfekte Wahl. Sie bietet vollen Zugriff auf Analysedaten und Dashboards, ohne den Zugriff auf systemkritische Menüs wie Abrechnungen oder globale Einstellungen zu gewähren.
        </Tip>

        <Note>
            **Demnächst verfügbar:** Wir arbeiten daran, Ihnen die Möglichkeit zu geben, eigene granulare Benutzerrollen zu erstellen, um Ihnen noch mehr Kontrolle zu geben.
        </Note>
    </Step>

    <Step title="Die Einladung versenden">
        Geben Sie den Namen und die E-Mail-Adresse des neuen Mitglieds ein, wählen Sie die Rolle aus und versenden Sie die Einladung. Die Person erhält eine E-Mail, um Ihrem Workspace beizutreten.

        Sobald die Einladung angenommen wurde, erscheint das Mitglied im `Aktiv`-Reiter und Sie sind fertig!
    </Step>
</Steps>

---

## Checkliste für den Abschluss der Einrichtung

Herzlichen Glückwunsch! Wenn Sie alle oben genannten Schritte befolgt haben, sollte Ihre Analytics-Implementierung abgeschlossen sein. Verwenden Sie diese Checkliste, um zu überprüfen, ob alles korrekt konfiguriert ist:

<Tip>
    Sobald dies alles erledigt ist, können Sie es an das Marketing-Team übergeben. Falls Sie Ihre Kollegen noch nicht zur Plattform hinzugefügt haben, finden Sie hier eine Anleitung dazu.
</Tip>

<AccordionGroup>
    <Accordion title="✅ Script-Integrations-Überprüfung">
        - Analytics-Script ist im `<head>`-Bereich aller Seiten hinzugefügt
        - Script enthält die korrekte `data-website-id` Kennung
        - **URLs mit personenbezogenen Daten sind via `data-anonymize-url` anonymisiert**
        - Script lädt erfolgreich (überprüft im Dashboard oder Network-Tab)
        - Echtzeit-Seitenaufrufe erscheinen im Dashboard
        - Script ist auf allen Subdomains/Seiten vorhanden, die Tracking benötigen
    </Accordion>

    <Accordion title="✅ Event-Tagging-Überprüfung">
        - Wichtige Conversion-Punkte sind getaggt (CTAs, Formulare, Downloads)
        - Event-Namen folgen konsistenten Benennungskonventionen
        - Events werden beim Testen im Browser korrekt ausgelöst
        - Events erscheinen im Echtzeit-Event-Stream des Dashboards
    </Accordion>

    <Accordion title="✅ Dashboard-Konfiguration">
        - Teammitglieder haben entsprechenden Dashboard-Zugriff
        - E-Mail-Berichte sind eingerichtet (demnächst verfügbar)
    </Accordion>
</AccordionGroup>

---

## So sieht Ihre Analytics aus, wenn alles funktioniert

Sobald Ihre Einrichtung abgeschlossen ist und Daten fließen, können Sie Folgendes in Ihrem Dashboard erwarten:

<Frame caption="Echtzeit-Dashboard mit Live-Seitenaufrufen und Events">
    <img src="/images/mcfit.jpeg" alt="Analytics-Dashboard zeigt Echtzeit-Besucheraktivität, aktuelle Seitenaufrufe und aktive Events" />
</Frame>

---

## Benötigen Sie Hilfe?

Kontaktieren Sie kontakt@bchic.studio für Unterstützung.